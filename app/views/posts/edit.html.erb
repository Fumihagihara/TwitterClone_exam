<h2>投稿内容を編集する</h2>
<%= form_with(model: @post, local: true) do |form| %>
  <div class="post_content">
    <%= form.label :content %>
    <%= form.text_field :content %>
  </div>
  <%= form.submit %>
<% end %>
